global:
  scrape_interval: 15s

scrape_configs:

  - job_name: 'prometheus-srv'

    static_configs:
      - targets:
          - node-exporter:9100

  - job_name: node-exporters

    file_sd_configs:
      - files:
          - /etc/prometheus/targets/*.yml